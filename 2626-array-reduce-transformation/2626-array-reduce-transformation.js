/**
 * @param {number[]} nums
 * @param {Function} fn
 * @param {number} init
 * @return {number}
 */

//reduce to following expression

const reduce = (nums,fn,init) =>{
   return nums.reduce(fn,init)
}